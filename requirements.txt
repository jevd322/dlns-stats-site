# Core Flask web framework and extensions
Flask>=2.3.0
Flask-Compress>=1.10.0
Flask-Caching>=2.0.0
Flask-CORS>=4.0.0

# Environment and configuration
python-dotenv>=1.0.0

# HTTP requests for external APIs
requests>=2.28.0

# Markdown processing for updates page
markdown>=3.5.0

# Production server (optional - for deployment)
gunicorn>=20.1.0
waitress>=2.1.0