import React from 'react'
import ReactDOM from 'react-dom/client'
import { SoundLibrary } from '../main.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SoundLibrary />
  </React.StrictMode>,
);
