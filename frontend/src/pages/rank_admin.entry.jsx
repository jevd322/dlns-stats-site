import React from 'react';
import ReactDOM from 'react-dom/client';
import { RankAdmin } from './rank_admin.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RankAdmin />
  </React.StrictMode>,
);
